# Ignorer les dossiers de données (Volumes Docker)
data/
**/data/

# Ignorer les fichiers de secrets Ansible (non chiffres)
# Note: vault.yml est chiffre (AES256) et DOIT etre dans le repo
# pour que le deploy CI/CD fonctionne
secrets.yml

# Ignorer les fichiers de configuration contenant des mots de passe
config.yaml
.env
-env.*

# Ignorer les fichiers temporaires
*.log
.ansible_cache/

#autre
.idea/
.vscode/
.DS_Store
node_modules/
__pycache__/
*.log
*.sqlite3
*.db

.venv/
