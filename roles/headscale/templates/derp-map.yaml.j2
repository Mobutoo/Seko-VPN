# ============================================================
# Carte DERP personnalisée - Remplace les serveurs Tailscale Inc
# Géré par Ansible - NE PAS MODIFIER MANUELLEMENT
# ============================================================
# Cette carte définit les serveurs relais DERP utilisés quand
# une connexion WireGuard directe (peer-to-peer) est impossible.
#
# Le serveur DERP intégré de Headscale est ajouté automatiquement
# via la config (automatically_add_embedded_derp_region: true).
#
# Vous pouvez ajouter d'autres serveurs DERP ici si vous avez
# des relais dans d'autres régions géographiques.
# ============================================================

# Carte DERP vide par défaut - le serveur intégré suffit pour
# un déploiement mono-serveur. Ajoutez des régions si vous avez
# besoin de relais supplémentaires.

regions:
  # Exemple de région DERP supplémentaire (décommenter si besoin) :
  # 901:
  #   regionid: 901
  #   regioncode: "eu-west"
  #   regionname: "Europe West - Custom"
  #   nodes:
  #     - name: "eu-west-1"
  #       regionid: 901
  #       hostname: "derp-eu.example.com"
  #       ipv4: "198.51.100.10"
  #       stunport: 3478
  #       stunonly: false
  #       derpport: 443
