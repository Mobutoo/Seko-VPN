---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: monit-debian12
    image: trfore/docker-debian12-systemd
    pre_build_image: true
    privileged: true
    cgroupns_mode: host
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
provisioner:
  name: ansible
verifier:
  name: ansible
