# Monitoring Alloy — Géré par Ansible
check process alloy matching "alloy"
    start program = "/usr/bin/systemctl start alloy"
    stop program = "/usr/bin/systemctl stop alloy"
    if 3 restarts within 5 cycles then exec "/etc/monit/telegram-alert.sh 'Alloy instable'"
