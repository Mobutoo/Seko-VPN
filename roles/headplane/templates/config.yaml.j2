# config.yaml Headplane — Géré par Ansible
server:
  host: "0.0.0.0"
  port: 3000
  base_url: "https://{{ domain_headplane }}"
  cookie_secret: "{{ vault_headplane_cookie_secret }}"
  cookie_secure: true
  data_path: "/var/lib/headplane"

headscale:
  url: "http://headscale:8080"
  config_path: "/etc/headscale/config.yaml"
  config_strict: true

integration:
  docker:
    enabled: true
    container: headscale
